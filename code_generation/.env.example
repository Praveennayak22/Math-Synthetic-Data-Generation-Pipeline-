# 1. The Brain — Model via TensorStudio
export TENSORSTUDIO_API_KEY="<your API key>"
export LLM_URL="<Model URL>"
export LLM_MODEL="<Model Name>"

# 2. The Generator / Verifier — Model on the cluster
# Pointing directly to the internal cluster node (No tunnel needed!)
export GEN_LLM_URL="<Internal cluster node>"
export GEN_LLM_MODEL="<Model Name"

# 3. Parallelism — tune based on cluster capacity / API rate limits
export GEN_PARALLEL_SEEDS=8      # concurrent seeds in generate_variants.py
export COMPLETION_PARALLEL=8     # concurrent Brain calls in completion_tensorstudio.py
